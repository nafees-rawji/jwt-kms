{
  "name": "jwt-kms-typescript",
  "version": "0.0.2",
  "description": "Sign and validate JWT tokens using keys stored in the AWS Key Management Service (KMS).",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "tslint": "^6.1.3",
    "typescript": "^3.9.7",
    "@types/node": "^14.11.8"
  },
  "dependencies": {
    "aws-sdk": "^2.264.1",
    "base64url": "^3.0.0"
  },
  "repository": {
    "type" : "git",
    "url" : "https://github.com/nafees-rawji/jwt-kms.git"
  },
  "license": "MIT"
}
